import 'package:flutter/material.dart';

class PostLostPet extends StatelessWidget {
  const PostLostPet({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
