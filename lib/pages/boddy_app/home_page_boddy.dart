import 'package:flutter/material.dart';

class HomePageBoddy extends StatelessWidget {
  const HomePageBoddy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.red,
    );
  }
}
