import 'package:flutter/material.dart';

class ServicesBoddy extends StatelessWidget {
  const ServicesBoddy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.orange,
    );
  }
}
