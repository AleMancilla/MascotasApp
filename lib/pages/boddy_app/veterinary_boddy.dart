import 'package:flutter/material.dart';

class VeterinaryBoddy extends StatelessWidget {
  const VeterinaryBoddy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
