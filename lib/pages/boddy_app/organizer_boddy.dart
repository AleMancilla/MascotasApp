import 'package:flutter/material.dart';

class OrganizerBoddy extends StatelessWidget {
  const OrganizerBoddy({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}
